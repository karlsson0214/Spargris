using System;

namespace DemoSpargris
{
    class Spargris
    {
        // objektvariabler
        private string ägare;
        private int belopp;

        // konstruktor - ny
        public Spargris(string namn)
        {
            ägare = namn;
        }
    }
}